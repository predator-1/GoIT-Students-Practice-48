// Шаблонні рядки
const itemName = 'Cyber Systems';
const cost = 70;
const count = 3;

// const sentence = `${itemName} cost ${cost} millions and we have ${count} items`;
const sentence =
  itemName + ' cost ' + cost + ' millions and we have ' + count + ' items';
console.log(sentence);
